# Copy to .env and fill in from Supabase: Project Settings -> API.
# The anon/publishable key is meant to ship in clients; RLS protects the data.
# Leave these empty to run the extension fully local (no sign-in, no sync).
WXT_SUPABASE_URL=https://YOUR-PROJECT-REF.supabase.co
WXT_SUPABASE_ANON_KEY=
